First use of operator in # Strings

## Task

In this step we will learn the use of assignment operator in strings
write python **code**

`data = 'hello world'
print(data[0])
print(len(data))
print(data)`{{execute}}